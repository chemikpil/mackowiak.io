import styled from 'theme/styled';

const Who = styled.h2`
    margin: 0;

    font-size: calc(1rem + (16 - 1) * ((100vw - 300px) / (1600 - 300)));
    font-weight: normal;
    text-indent: -0.1vw;
`;

export { Who };
